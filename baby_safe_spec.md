# ベビーセーフ・ルーム（仮）仕様書 v3.3

Claude Codeに渡して実装することを前提にした仕様書。
**フェーズ1（2D・音なし）を完成させてから、同じゲームロジックを使ってフェーズ2（3D・効果音・アニメーション）へ移行する。**
フェーズ2で描画層だけを差し替えられるよう、フェーズ1の時点からゲームロジックと描画を分離した構成で実装すること。

v2からの変更点：おもちゃと満足度メーターの追加、組み合わせ危険、ドラッグ操作、赤ちゃんの重み付きランダム移動、教育カードの出典確定。
v3.1の変更点：赤ちゃんへの直接介入（抱き上げ・取り上げ）と、その反動としての「ぐずり状態」を追加。
v3.2の変更点：おもちゃの「飽きて手放す」「同じおもちゃへの慣れ」、ロード・遷移中の豆知識表示を追加。
v3.3の変更点：全ルールに即時の因果表現を義務化（§4.8）、ヘッドレス自動テストプレイ（§13）を追加。

---

## 0. 実装者（Claude Code）への指示

- フェーズ1を §11 の受け入れ条件まで完成させ、動作確認してからフェーズ2に着手する
- ゲームロジック（`src/game/`）は両フェーズで **同一ファイルを共有** する。フェーズ2でロジックを書き換える必要が出たら設計ミスなので、先にフェーズ1側を直す
- 3Dモデル・音声ファイルは **外部AIツールで別途用意する**（§9）。ファイルが存在しない場合はプレースホルダー（箱形状・合成音）で動く実装にする
- 教育カードの文言（§8）は **仕様書の文言・出典をそのまま使う**。実装者が数値や表現を足さない
- フェーズ1のステップ5が終わった時点で §13 のシミュレーションハーネスを作り、以降の難易度調整は **人間に遊ばせる前にシミュレーションで指標を目標範囲に入れる**
- 各フェーズの最後に `README.md` に起動手順・素材の置き場所・ライセンス表記欄・`npm run sim` の使い方を書く

---

## 1. 概要

| 項目 | 内容 |
|---|---|
| ジャンル | トップビュー / 制限時間内に部屋を安全にしつつ、赤ちゃんを退屈させないパズル寄りアクション |
| 目的 | 最近子供が産まれた社員へのプレゼン用。遊びながら家庭内事故（起きて動いている赤ちゃんの事故）の予防知識を得られる |
| 扱わない範囲 | 睡眠中の事故（うつぶせ寝・寝具による窒息）、屋外・水回りの事故 |
| プレイ時間 | 1ステージ約90秒 × 3ステージ + 結果画面 = 5〜7分 |
| 対象環境 | PCブラウザ（Chrome最新）。マウス操作のみ |
| 配布形態 | フェーズ1：`index.html` 1ファイル。フェーズ2：Vite でビルドした `dist/` |
| 技術 | フェーズ1：Vanilla JS + Canvas 2D。フェーズ2：Three.js + Web Audio API。Vite。TypeScriptは使わない |

---

## 2. アーキテクチャ（両フェーズ共通）

```
src/
  game/            ← ゲームロジック。DOM・Canvas・Three.jsを一切参照しない
    state.js         状態機械（Title / Loading / Tutorial / Play / StageResult / FinalResult）
    stages.js        ステージ定義データ（§5）＋難易度変数
    objects.js       部屋のオブジェクト（hazard / toy / item）の状態遷移
    combos.js        組み合わせ危険の判定（§4.4）
    baby.js          赤ちゃんの目標選択（重み付きランダム）・移動・衝突・満足度
    scoring.js       ヒヤリ判定・スコア・クリア判定
    edu.js           教育カード・豆知識のテキスト（§8）
  render/
    IRenderer.js     描画層インターフェース
    canvas2d/        フェーズ1 実装
    three/           フェーズ2 実装
  audio/
    IAudio.js        音声層インターフェース
    silent.js        フェーズ1 実装
    webaudio.js      フェーズ2 実装
  input.js         マウスイベント → 「長押し開始/終了」「ドラッグ開始/移動/終了」＋対象オブジェクトid に変換
  main.js          ループ・DI
sim/               ← ヘッドレス・シミュレーション（§13）。src/game/ だけを import する
  bots/            戦略別ボット
  run.js           バッチ実行と指標集計
  assert.js        受け入れ条件の自動判定
```

### 描画層インターフェース

```js
class IRenderer {
  init(container)
  loadStage(stage)
  update(gameState, dt)
  pickObject(clientX, clientY)        // 画面座標 → object.id | null
  toGameCoords(clientX, clientY)      // 画面座標 → ゲーム座標 {x, y}（ドラッグ先の決定に使う）
  playEffect(type, objectId, payload) // "fixed" | "removed" | "hiyari" | "respawn" | "play_done" | "combo_warn"
                                      // | "pickup" | "takeaway" | "fuss_start" | "combo_hiyari" | "bored" | "unbored"
                                      // | "sat_delta"（payload: {amount}）| "no_toy"（payload: {active}）
  showOverlay(kind, payload)          // HUD外のUIはHTMLで重ねる
  dispose()
}
```

### 音声層インターフェース

```js
class IAudio {
  init()                         // ユーザー操作後に呼ぶ
  play(name)                     // §9.2 の name
  startBgm(name) / stopBgm()
  setMuted(bool)
}
```

座標系は両フェーズで **ゲーム内座標（部屋を 800×540 の平面とみなす）** を共有する。フェーズ2ではゲーム座標 (x, y) を 3D の (x, 0, y) に写像する。

---

## 3. プレイヤーの操作

操作は **長押し** と **ドラッグ** の2種類のみ。チュートリアルで説明するのもこの2つだけ。

| 操作 | 対象 | アクション | 効果 |
|---|---|---|---|
| 長押し | hazard（固定物：コンセント・階段・引き出し等） | **塞ぐ** | `fixSec` 秒で `fixed` に。途中で離すと進捗リセット |
| 長押し | item（床の小物：電池・買い物袋等） | **片付ける** | `fixSec` 秒で消える |
| 長押し | toy（おもちゃ） | **片付ける** | 消える。満足度の回復手段が減る |
| ドラッグ | toy（床にある時のみ） | **置く／移す** | 任意の床位置へ移動。赤ちゃんの目標選択に強い重みが乗る（誘導材） |
| ドラッグ | baby | **抱き上げて移す** | 任意の床位置へ移動。満足度 −15、置いた位置で1秒停止、その後 **ぐずり**（§4.7） |
| 長押し（0.5秒） | baby（`carrying` がある時のみ） | **取り上げる** | 持っている toy が赤ちゃんの足元に落ちる（消えない）。満足度 −20、**ぐずり** |

- 長押し中に対象からカーソルが外れたら中断扱い
- ドラッグ先が `walls` の内側なら、最も近い床位置にスナップ
- ドラッグ中もゲームは進行する（ポーズしない）
- 対象の優先順位：赤ちゃんと toy が重なっている時は **赤ちゃんを優先**。赤ちゃんが持っている toy は単体では操作できない（取り上げてから）
- 抱き上げ・取り上げは回数制限なし。ただし **前回の介入から20秒以内** なら満足度の減少量を2倍にする（連打の抑止）
- 抱き上げ中（ドラッグ中）は赤ちゃんは移動せず、接触判定も行わない。ドロップした瞬間に再開

---

## 4. ゲームルール

### 4.1 コアループ

1. ステージ開始。部屋に hazard / item / toy が配置され、赤ちゃんが中央に出現する
2. 赤ちゃんは **重み付きランダム** で目標を選び、ハイハイで向かう（§4.5）
3. プレイヤーは長押しで塞ぐ／片付ける、ドラッグでおもちゃを置く
4. 赤ちゃんが未対策の hazard / item に **接触** すると「ヒヤリ」+1。赤ちゃんは中央に戻され1秒停止（`anim: "stun"`）
5. 赤ちゃんが toy に到達すると **3秒間遊ぶ**（`anim: "play"`）。完了で満足度回復＋スコア。その toy は `bored` 状態になり目標に選ばれない
6. ヒヤリ3回でステージ失敗。制限時間まで耐えるとクリア

### 4.1a おもちゃの飽きと慣れ

| ルール | 内容 |
|---|---|
| 飽き | 遊び終えた toy は `boredSec` 秒間 `bored`。1回目 30秒、2回目 40秒、3回目以降 50秒（`BORED_SEC_BASE + BORED_SEC_STEP × min(playCount-1, 2)`） |
| 慣れ | 同じ toy で遊ぶたびに満足度回復が減る：1回目 +25、2回目 +15、3回目以降 +10（`SAT_PLAY_BY_COUNT`）。`playCount` は toy ごとに保持し、別の toy で遊んでもリセットしない |
| 手放す | `carrying` の toy が `bored` になってから `DROP_AFTER_SEC`（8秒）経つと、その場に置いて手放す。`walls` 内なら最寄りの床にスナップ。手放した時点で目標を再選択 |
| 取り上げ後 | 足元に落ちた toy は `TAKEAWAY_BORED_SEC`（3秒）の間 `bored` 扱い（すぐ拾い直さない） |

設計意図：おもちゃが1個のステージ1では後半に満足度を維持しにくくなり、ステージ2以降で複数のおもちゃを回す動機が生まれる。飽きた toy を持ち歩き続けて組み合わせ危険を抱えたままになる状況も、8秒で自然解消する。

### 4.2 満足度メーター

- 0〜100。開始時 70
- 遊べる toy（飽きていない）が部屋に1つもない間、毎秒 `-4`
- 遊べる toy があるが遊んでいない間、毎秒 `-1`
- toy で遊び終えると `+25`（同じ toy の2回目以降は減る。§4.1a）
- **満足度 < 40 の間**：赤ちゃんの速度 +30%、目標選択で hazard/item への重み ×1.5（ぐずって探索が増える）
- 満足度 0 で即ヒヤリ+1 とはしない（メーターは間接ペナルティのみ）
- HUD にバー表示。40 を下回ると色が変わる

### 4.3 終了条件とスコア

- クリア判定：残り時間 0 かつ ヒヤリ < 3
- スコア = 残りヒヤリ猶予(0〜2) × 1000 + 遊び完了回数 × 200 + 全 hazard 対策完了までの残り秒数 × 10
- 全 hazard/item が対策・除去済みでも、toy がある限りゲームは制限時間まで続く（遊びで稼ぐ時間になる）

### 4.4 組み合わせ危険

`combos.js` に **明示列挙** する。動的な組み合わせ生成はしない。

```js
// 赤ちゃんが carrying の toy を持ったまま target の hazard に接触したとき、
// hazard が fixed でも requiresFixed が false なら ヒヤリ
const combos = [
  { toy: "spoon",   hazard: "outlet",    ignoresFix: true,  label: "金属スプーンをコンセントへ" },
  { toy: "ball",    hazard: "stairs",    ignoresFix: false, label: "ボールを追って階段へ" },
  { toy: "cloth",   hazard: "detergent", ignoresFix: false, label: "布で洗剤を包んで口へ" }
];
```

- 赤ちゃんは toy で遊び終えた後、その toy を **持ち歩く**（`baby.carrying = toyId`）。手放すのは「次の toy で遊んだ時」「ヒヤリで中央に戻った時」「飽きて8秒経った時」「取り上げられた時」
- `ignoresFix: true` の組み合わせは、対策済みの hazard でもヒヤリになる（「カバーをしたから安心」ではないことを学ぶ）
- 持っている toy と combo が成立する hazard に赤ちゃんが接近（距離 < 80）したら `playEffect("combo_warn")` で予告する
- 対策：該当 toy を片付ける、または遠くへドラッグする
- ステージ1では combo なし。ステージ2から導入

### 4.5 赤ちゃんの目標選択（重み付きランダム）

```
目標を選ぶタイミング: 到達時 / 目標が消えた時 / 5秒以上ほぼ移動していない時 / 満足度が40を跨いだ時 / 抱き上げから解放された時 / ぐずり開始・終了時
isHeld == true の間は移動・接触判定を行わない
候補: state=="open" の hazard・item、飽きていない toy（他の赤ちゃんの目標は除く）
重み w = base / (distance + 100)
  base: toy = 3.0（直前にドラッグされた toy は 5.0 を 8 秒間）
        hazard/item = 1.0（満足度 < 40 のとき 1.5、ぐずり中は 2.0。両方該当なら大きい方）
候補が無ければ 中央付近をゆっくり往復（anim: "idle"）
重みに比例した確率で1つ選ぶ
移動: 単位ベクトル × speed × dt。walls に当たれば x成分のみ/y成分のみを試す
接触判定: 距離 < 28
```

### 4.6 再発ギミック

一部の item は片付けた後 `respawnSec` 秒で再出現する（電池が転がってくる等）。再発3秒前から予告点滅。

### 4.7 ぐずり状態（介入の反動）

抱き上げ・取り上げの直後、赤ちゃんは `FUSS_SEC` 秒間「ぐずり」になる。

- 速度 ×1.5、目標選択で hazard/item への重み ×2.0（満足度 < 40 の効果とは **乗算せず、大きい方を採用**）
- ぐずり中は toy で遊んでも満足度回復が半分（+12）
- ぐずり中に再度介入すると、残り時間を `FUSS_SEC` にリセット（延長）
- 表現：頭上に不機嫌アイコン、モヤモヤ強め、低い短音（`fuss`）。泣き声・涙・倒れる描写は入れない
- HUD には「ぐずり」の残秒数バッジを表示

設計意図：介入は緊急手段として有効だが、使うほど危険が増える螺旋になり、「先に環境を整える方が楽」という結論をプレイヤーが体験で得る。

### 4.8 因果表現の原則（全ルール共通）

ルールは文章で説明せず、**状態が変わった瞬間に、原因と結果が1秒以内に画面上で結びつく**表現で学ばせる。以下の表は両フェーズで必須。描画層は `playEffect` と `gameState` から実装する。

| 状態変化 | 表現（フェーズ1 / フェーズ2 共通の要件） |
|---|---|
| 塞ぐ・片付ける完了 | 進捗リングが満ちて ✅ に変わる。対象の色が赤系→緑系 |
| toy で遊び終えた | toy の上に **回復量の数字**（+25 / +15 / +10）が0.8秒ポップし、満足度バーが同時に伸びて光る |
| toy が bored になった | toy に「zzz」マークが付く。bored 解除で消える |
| 遊べる toy が1つも無い | 満足度バーの横に「あそべるものがない」アイコン（点滅なし）。減衰中はバーが薄い色で減る |
| ぐずり開始 | 頭上に不機嫌アイコン、モヤモヤ増加、低い短音。HUD に残秒バッジ |
| 組み合わせ危険の接近 | 持っている toy と対象 hazard が同じ色で同期点滅 |
| 組み合わせ危険でヒヤリ | 通常のヒヤリ表現に加え、画面中央に **「スプーン × コンセント」** のように組み合わせ名を1.5秒表示（`combo.label` を使う） |
| 対策済み hazard でヒヤリ（ignoresFix） | 上記に加え、✅ マークが一瞬揺れる（「塞いだのに」を見せる） |
| 再発 | 予告点滅 → 出現時に小さく跳ねる |
| 介入（抱き上げ・取り上げ） | 満足度バーの横に **減少量の数字**（−15 / −20 / −40）を赤系でポップ |

- 数字のポップは全て同じフォント・同じ動き（上に20px移動しながらフェード）。良い変化は緑系、悪い変化は赤系
- テキストは名詞のみ（「あそべるものがない」「スプーン × コンセント」）。説明文・命令文は出さない
- Tutorial 画面の文言は3行以内。ルールの網羅ではなく操作の紹介に留める

---

## 5. ステージ設計

### ステージ1 リビング（チュートリアル）

- 90秒、赤ちゃん1人、toy 1個、combo なし、再発なし
- 開始前に操作説明：「危険な場所を長押しで塞ぐ」「おもちゃはドラッグで置ける」「赤ちゃんもドラッグで移せるが、嫌がって危険が増える」「3回ヒヤリで失敗」

| id | 種別 | 名称 | 事故種別 | 対策 | fixSec |
|---|---|---|---|---|---|
| outlet | hazard | コンセント | 感電 | コンセントカバー | 1.0 |
| stairs | hazard | 階段 | 転落 | ベビーゲート | 2.0 |
| table | hazard | テーブルの角 | 打撲 | コーナーガード | 1.0 |
| detergent | hazard | 洗剤ボトル | 誤飲 | 高い棚へ移す | 1.5 |
| drawer | hazard | 引き出し | 指はさみ | チャイルドロック | 1.5 |
| battery | item | ボタン電池 | 誤飲 | 拾って捨てる | 0.5 |
| ball | toy | ボール | — | — | 0.5（片付け） |

### ステージ2 キッチン

- 90秒、赤ちゃん1人、toy 2個、combo 2種、再発あり

| id | 種別 | 名称 | 事故種別 | 対策 | fixSec | 再発 |
|---|---|---|---|---|---|---|
| outlet | hazard | コンセント | 感電 | コンセントカバー | 1.0 | — |
| kettle | hazard | 電気ケトル | やけど | コードを束ねて奥へ | 1.5 | — |
| knife | hazard | 包丁 | 切創 | 引き出しに収納 | 1.0 | — |
| drawer | hazard | 引き出し | 指はさみ | チャイルドロック | 1.5 | — |
| trash | hazard | ゴミ箱 | 誤飲 | ロック付きに交換 | 1.5 | — |
| battery | item | ボタン電池 | 誤飲 | 拾って捨てる | 0.5 | 25秒 |
| grocery | item | 床の買い物袋 | 窒息 | 片付ける | 0.5 | 20秒 |
| spoon | toy | 金属のスプーン | — | — | 0.5 | — |
| ball | toy | ボール | — | — | 0.5 | — |

combo：`spoon × outlet`（ignoresFix）、`ball × stairs` は階段が無いため不成立。代わりに `ball × kettle`（ignoresFix: false、ボールを追ってケトルのコードに引っかかる）を追加。

### ステージ3 双子

- 75秒、赤ちゃん2人、ステージ1の配置 + `spoon` `cloth` を追加、combo 3種すべて、`battery` 再発20秒
- 2人は別々の目標を選ぶ

### 難易度変数（`stages.js` 先頭に集約）

```js
export const TUNING = {
  BABY_SPEED: 55, BABY_SPEED_STAGE3: 65, BORED_SPEED_MULT: 1.3,
  PLAY_SEC: 3, BORED_SEC_BASE: 30, BORED_SEC_STEP: 10, DROP_AFTER_SEC: 8, TAKEAWAY_BORED_SEC: 3,
  SAT_START: 70, SAT_NO_TOY: -4, SAT_IDLE: -1, SAT_PLAY_BY_COUNT: [25, 15, 10], SAT_LOW: 40,
  WEIGHT_TOY: 3.0, WEIGHT_TOY_DRAGGED: 5.0, WEIGHT_HAZARD: 1.0, WEIGHT_HAZARD_BORED: 1.5,
  TOUCH_DIST: 28, COMBO_WARN_DIST: 80,
  PICKUP_SAT: -15, TAKEAWAY_SAT: -20, TAKEAWAY_HOLD_SEC: 0.5,
  INTERVENE_REPEAT_SEC: 20, INTERVENE_REPEAT_MULT: 2,
  FUSS_SEC: 5, FUSS_SPEED_MULT: 1.5, FUSS_HAZARD_WEIGHT: 2.0, FUSS_PLAY_SAT: 12
};
```

---

## 6. 画面構成と状態遷移

```
[Title] --Start--> [Loading(tip)] --> [Tutorial] --OK--> [Play(stage1)]
[Play] --clear--> [StageResult] --Next--> [Loading(tip)] --> [Play(stage n+1)]
[Play] --fail---> [StageResult] --Retry--> [Loading(tip)] --> [Play(same stage)]
[StageResult(stage3 clear)] --> [FinalResult] --Title--> [Title]
```

| 状態 | 表示内容 |
|---|---|
| Title | タイトル、Start、（フェーズ2）ミュート |
| Loading | 豆知識1件（§8）＋進行インジケータ。最低2.5秒。フェーズ2はここで glTF を読む |
| Tutorial | 操作説明1枚（長押し・ドラッグ・ヒヤリ3回）＋下部に豆知識1件 |
| Play | ゲーム画面 + HUD（残り時間 / ヒヤリ ○○○ / 満足度バー / ぐずり残秒バッジ / ステージ名） |
| StageResult | クリア/失敗、スコア内訳、**教育カード1枚**、Next/Retry |
| FinalResult | 合計スコア、**まとめカード**、Titleへ |

HUD・結果画面・カードは両フェーズとも **HTML要素をゲーム画面に重ねて表示** する。

---

## 7. データ構造

```js
const object = {
  id: "outlet",
  kind: "hazard",            // "hazard" | "item" | "toy"
  label: "コンセント", accident: "感電", fix: "コンセントカバー",
  emoji: "🔌",               // フェーズ1
  model: "outlet", fixedModel: "outlet_cover",   // フェーズ2
  x: 120, y: 420,
  fixSec: 1.0, respawnSec: null,
  draggable: false,          // toy のみ true
  // 実行時
  state: "open",             // hazard/item: "open" | "fixing" | "fixed" | "removed"
                             // toy: "available" | "bored" | "removed"
  progress: 0, respawnAt: null, boredUntil: null, draggedAt: null,
  playCount: 0               // toy のみ。慣れの計算に使う
};

const stage = {
  id: 1, name: "リビング", timeLimit: 90, babies: 1,
  walls: [ {x, y, w, h, model: "sofa"}, ... ],
  objects: [ ... ],
  combos: [ ... ],           // §4.4 の形式。このステージで有効なもののみ
  eduCardId: "battery"
};

const baby = {
  x: 400, y: 270, speed: 55,
  targetId: null, carrying: null, carriedBoredAt: null,   // carrying が bored になった時刻
  playUntil: 0, stunUntil: 0,
  satisfaction: 70,
  fussUntil: 0, lastInterventionAt: -Infinity,
  isHeld: false,             // ドラッグ中 true
  anim: "crawl"              // "crawl" | "idle" | "stun" | "play" | "fuss" | "held"
};
```

---

## 8. 教育コンテンツ

各ステージ後に1枚、最終画面に1枚。**以下の文言と出典を変更せずに使う。**

### カード battery（ステージ1後）

> **ボタン電池は「短時間」が勝負**
> ボタン電池の誤飲事故は生後6か月〜2歳に多く起きています。飲み込むと電気分解でアルカリが生じて消化管を傷つけ、コイン形リチウム電池では30分〜2時間ほどで潰瘍ができると報告されています。電池を使う製品ごと、手の届かない場所へ。
>
> 出典：日本中毒情報センター「ボタン電池による小児の事故」／製品評価技術基盤機構（NITE）「電池の誤飲」／国民生活センター 報道発表 2024年7月31日

### カード burn（ステージ2後）

> **やけどの多くは「つかまり立ち」と「コード」から**
> 医療機関から消費者庁に寄せられた情報では、2020年12月までの約10年間に、炊飯器や電気ケトルなどによる2歳以下のやけど事故が333件ありました。ケトルのコードを引っ張って倒す、炊飯器につかまり立ちして転倒するといった事例が報告されています。転倒時に湯がこぼれにくい製品を選び、コードごと手の届かない場所へ。
>
> 出典：政府広報オンライン「乳幼児のやけど事故にご注意を！」／こども家庭庁「こどもを事故から守る！事故防止ハンドブック」／消費者庁 子ども安全メール Vol.645（2024年2月7日）

### カード combo（ステージ3後）

> **「カバーをしたから安心」ではない**
> ヘアピンや鍵など、身近な金属をコンセントに差し込んで感電・やけどをした事故が医療機関から報告されています（約6年間で約30件）。件数はやけどや誤飲より少ないものの、家庭用の100Vでも心臓に影響する恐れがあります。危険な場所を塞ぐことと、危険になり得る小物を床に置かないことは、別々の対策です。
>
> 出典：消費者庁 子ども安全メール Vol.568（2021年10月18日）／日本経済新聞 2017年4月3日（消費者庁への取材）

### カード summary（最終画面）

> **今日できるチェックリスト**
> ・コンセントにカバー、床に金属の小物を置かない
> ・電気ケトル・炊飯器はコードごと手の届かない場所へ
> ・ボタン電池を使う製品はフタが開かないか確認
> ・洗剤・化粧品は高い棚へ
> ・遊べるおもちゃがある部屋は、危険への探索が減ります
>
> このゲームは、起きて動いている赤ちゃんの事故だけを扱っています。睡眠中の事故については、こども家庭庁の事故防止ハンドブックを参照してください。

### 表示ルール

- 全カード末尾に固定文：「公的資料の要約であり、すべての危険を網羅するものではありません」
- 恐怖を煽る表現・写真・赤ちゃんが倒れる/泣く描写は入れない。ヒヤリ演出は文字と短い効果のみ
- 出典はテキスト表示のみ（リンクボタンは付けない）

### 豆知識（ロード・遷移中に1件ずつ表示）

`edu.js` に `tips` 配列として持つ。各要素は `{ id, text, source, verified }`。**`verified: false` のものは、表示前に出典を確認して true にするか削除する。** 実装者が文言を追加・変更しない。

表示タイミング：
- Title → Tutorial、StageResult → 次ステージ開始の遷移中（フェーズ1は最低 2.5秒 表示してから進む。フェーズ2は glTF 読み込み完了まで、ただし最低 2.5秒）
- Tutorial 画面の下部にも1件
- 同一プレイ中は重複させない（表示済み id を保持。全部出したらリセット）
- 1件 60〜90字。出典は小さく併記

| id | text | source | verified |
|---|---|---|---|
| tip_crawl_age | ハイハイを始める時期は個人差が大きく、生後7〜10か月ごろが多いとされます。この時期から行動範囲が急に広がります。 | 厚生労働省 乳幼児身体発育調査（ハイハイの通過率） | false（調査年と数値を確認） |
| tip_battery_age | ボタン電池の誤飲は生後6か月〜2歳に多く、2歳を過ぎると「鼻に入れる」事故の方が増えます。 | 日本中毒情報センター「ボタン電池による小児の事故」 | true |
| tip_battery_time | コイン形リチウム電池は、飲み込んでから30分〜2時間ほどで消化管に潰瘍を作ると報告されています。 | 製品評価技術基盤機構（NITE）「電池の誤飲」 | true |
| tip_battery_package | 2018年に国内の電池業界で、誤飲防止パッケージが採用されています。古い電池の保管にも注意。 | 国民生活センター 報道発表 2024年7月31日 | true |
| tip_kettle_lock | 転倒時に湯がこぼれにくい電気ケトルでも、ロックをかけていないと湯がこぼれた事例が報告されています。 | 消費者庁 子ども安全メール Vol.645（2024年2月7日） | true |
| tip_kettle_floor | 電気ケトルを床に直接置く習慣は、こどもが触れて転倒させる事故の背景として挙げられています。 | 消費者庁 コラム Vol.11「転倒時に湯漏れしにくい電気ケトルの使用を!」 | true |
| tip_outlet_metal | コンセントの感電事故で差し込まれた物は、ヘアピン・鍵・クリップなど身近な金属が多いと報告されています。 | 消費者庁 子ども安全メール Vol.568（2021年10月18日）／日本経済新聞 2017年4月3日 | true |
| tip_outlet_wet | 唾液や汗で濡れた手は電気を通しやすく、こどもがコンセントに触れる危険を高めます。 | 消費者庁 子ども安全メール Vol.568 | true |
| tip_skin | こどもの皮膚は大人より薄く、同じ温度の湯でも深いやけどになりやすいとされています。 | 消費者庁 子ども安全メール Vol.645 | true |
| tip_home | 交通事故を除くと、こどもの事故の発生場所は家庭内がほとんどを占めます。 | 消費者庁「子どもの不慮の事故の発生傾向」（2022年3月23日、人口動態調査より） | true |
| tip_habituation | 乳児は同じ刺激に繰り返し触れると反応が弱まり（馴化）、新しい刺激には注意が戻ります。同じおもちゃに飽きるのはこの性質です。 | 発達心理学の一般的知見（馴化・脱馴化パラダイム） | false（教科書等の具体出典を付ける） |
| tip_object_permanence | 生後8〜9か月ごろから「見えなくなった物もそこにある」と理解し始め、物を取り上げられることへの抗議が強くなるとされます。 | 発達心理学の一般的知見（対象の永続性） | false（具体出典を付ける） |
| tip_environment | 事故予防の資料では、「見守り」より「環境を変える」対策（ゲート・ロック・配置の変更）が優先して勧められています。 | こども家庭庁「こどもを事故から守る！事故防止ハンドブック」 | true |

- 統計の「件数」は医療機関ネットワーク（参画医療機関からの報告）に基づくもので、全国の発生件数ではない。件数を出す tip には「医療機関から報告された」を必ず含める

---

## 9. フェーズ2：3D・効果音・アニメーション

### 9.1 描画（Three.js）

- カメラ：固定俯瞰（斜め上 60° の PerspectiveCamera）。回転操作なし
- 照明：AmbientLight + DirectionalLight 1つ。影は DirectionalLight のみ
- 部屋：床 PlaneGeometry、壁 BoxGeometry。家具・オブジェクトは glTF（`assets/models/*.glb`）
- **プレースホルダー**：glb が無い場合は BoxGeometry に kind 別の色（hazard=赤系、item=橙系、toy=青系）＋ `label` の Sprite。素材が無くても全ステージ遊べること
- 赤ちゃん：glTF + AnimationMixer。`baby.anim` でクリップ切替（`crawl` `idle` `stun` `play` `fuss` `held`。後2つは省略可）。クリップが無ければ上下に揺れる手続きアニメ。`carrying` がある時は toy のメッシュを赤ちゃんの前方に追従させる
- ドラッグ：Raycaster で toy を拾い、床平面（y=0）との交点をドラッグ先にする（`toGameCoords`）
- 演出：
  - 設置中：オブジェクト上空にリング状進捗ゲージ
  - 対策完了：緑のパーティクル上昇（0.6秒）、`fixedModel` に差し替え
  - 遊び中：toy 上空に小さな星のパーティクルが回る。完了時に満足度バーが光る
  - ヒヤリ：画面全体を薄い黄色で 0.2 秒フラッシュ。HUD の ○ が × に
  - combo 予告：対象 hazard と持っている toy の両方が同じ色で点滅
  - 再発予告：emissive の周期変化
- 満足度 < 40：赤ちゃんの周囲にモヤモヤ（半透明の小球3つ）が漂う
- ぐずり中：モヤモヤを6つに増やし、頭上に不機嫌アイコンの Sprite。`anim: "fuss"` のクリップがあれば再生、無ければ左右に小さく揺れる手続きアニメ
- 抱き上げ中：赤ちゃんを床から少し持ち上げ（y=+20）、影を残す。`anim: "held"`。ドロップで着地の短いスケールアニメ
- 取り上げ：toy が赤ちゃんの手元から足元へ弧を描いて落ちる（0.4秒）
- パフォーマンス：合計ポリゴン 10万以下。読み込み時に頂点数をログ

### 9.2 音声（Web Audio API）

| name | 用途 | 長さ目安 |
|---|---|---|
| click | 長押し開始 / ドラッグ開始 | 0.1s |
| fix_done | 塞ぐ・片付ける完了 | 0.5s |
| play_done | 遊び完了 | 0.7s（明るい） |
| hiyari | 接触 | 0.4s（低め。驚かせない） |
| combo_warn | 組み合わせ予告 | 0.3s |
| pickup | 抱き上げ開始 | 0.2s（柔らかい） |
| fuss | ぐずり開始 | 0.4s（低め。泣き声ではない） |
| respawn | 再発 | 0.5s |
| clear / fail | ステージ終了 | 1〜2s |
| bgm_main | プレイ中ループ | 30〜60s |
| bgm_bored | 満足度 < 40 の間、bgm_main に重ねる軽いレイヤー（省略可） | ループ |

- ファイルは `assets/audio/*.mp3`（または .ogg）。**無い場合は OscillatorNode の合成音で代替**
- 初回クリックまで AudioContext を生成しない
- ミュートトグルを Title と HUD に置く。設定は localStorage 保存可

### 9.3 素材の準備（人間側の作業）

| 種類 | 手段（候補） | 指示 |
|---|---|---|
| 家具・オブジェクトモデル | Meshy / Tripo / Rodin | 全モデル共通接頭辞「low-poly, flat-shaded, toy-like, pastel colors」。glb 書き出し。1モデル 5,000ポリゴン以下（超えたら Blender の Decimate） |
| 赤ちゃんモデル + アニメ | Meshy 自動リグ、または Mixamo | クリップ名 `crawl` `idle` `stun` `play`。クロールが崩れる場合は `idle` のみ用意し移動は手続きアニメに任せる |
| 効果音 | ElevenLabs Sound Effects、または Freesound.org（CC0） | 「toy-like, soft, not scary」。hiyari は低めの音 |
| BGM | Suno / Udio、または Freesound（CC0） | 明るいループ、歌なし。社内利用に留め、公開時は CC0 に差し替える |

- 生成AIの利用規約・ライセンスを利用時点で確認し、`README.md` に記載する

---

## 10. 実装順

### フェーズ1（2D）— 目安1.5日

1. **骨格**：Vite、`src/game/` の状態機械、ループ、dt。`IRenderer`/`IAudio` 定義、`canvas2d`/`silent` 差し込み。**乱数はシード指定可能な rng を注入**（`Math.random` を直接呼ばない）
2. **部屋描画**：ステージ1の walls と objects（kind 別の色・絵文字）。HUD（HTML）
3. **赤ちゃん移動**：重み付きランダム目標選択、直線移動、壁との滑り、引っかかり対策
4. **長押し**：塞ぐ／片付ける。進捗リング。完了で fixed/removed
5. **ヒヤリと終了判定**：接触、カウント、タイマー、Result画面
5b. **シミュレーションハーネス**：`sim/run.js`、`noop` と `optimal` ボット、指標出力（§13.1〜13.3 の一部）。以降のステップで機能が増えるたびにボットと指標を追随させる
6. **おもちゃと満足度**：toy への到達→3秒遊ぶ→bored。飽き時間の段階増加・慣れによる回復減少・8秒後の手放し。満足度の増減とHUDバー。速度・重みへの反映
7. **ドラッグ**：toy の移動、壁スナップ、ドラッグ直後の重み増加
7b. **赤ちゃんへの介入**：抱き上げ（ドラッグ・isHeld）、取り上げ（長押し0.5秒・toy が足元に落ちて3秒 bored）、満足度減少と20秒ルール、ぐずり状態（速度・重み・回復半減・HUDバッジ）
8. **ステージ2・3**：定義追加、再発、combo（carrying・ignoresFix・予告）、赤ちゃん複数
9. **教育カードと豆知識**：§8 の文言をそのまま表示。Loading 状態（最低2.5秒）と重複回避
10. **因果表現と仕上げ**：§4.8 の表現を全て実装。Tutorial（3行以内）、スコア内訳、遷移。難易度調整は §13.4 の手順で行い、`npm run sim:assert` を通す。最後に人間が1〜2回遊ぶ

→ §11 フェーズ1 の受け入れ条件を確認。`vite build --base ./` で単一 `index.html` も出力。

### フェーズ2（3D・音）— 目安3〜4日

11. **Three.js 描画層**：プレースホルダーで全ステージが遊べる状態。Raycaster による `pickObject` と床平面ドラッグ
12. **演出**：進捗リング、パーティクル、フラッシュ、combo 予告点滅、モヤモヤ
13. **glTF 読み込み**：あれば差し替え、無ければプレースホルダー維持。`carrying` の追従
14. **赤ちゃんアニメ**：AnimationMixer と `baby.anim`
15. **音声層**：合成音フォールバック → ファイル再生。ミュート
16. **仕上げ**：カメラ・照明、ポリゴン数ログ、README ライセンス欄

---

## 11. 受け入れ条件

### フェーズ1
- [ ] `index.html` を開くだけで起動する
- [ ] ステージ1を何もしないと約30〜40秒で失敗する
- [ ] ステージ1を全対策し、ボールで2回以上遊ばせてクリアできる
- [ ] 長押し途中で離す／カーソルが外れると進捗がリセットされる
- [ ] toy をドラッグすると、8秒以内に赤ちゃんがそちらへ向かう確率が明らかに上がる（10回試して7回以上）
- [ ] ステージ2で spoon を持った赤ちゃんが対策済み outlet に触れるとヒヤリになり、事前に予告が出る
- [ ] 満足度が40を下回ると赤ちゃんが速くなり、バーの色が変わる
- [ ] 赤ちゃんをドラッグして移せる。ドラッグ中は移動・接触判定が止まり、ドロップ後1秒停止してからぐずりに入る
- [ ] toy を持った赤ちゃんを0.5秒長押しすると toy が足元に落ち、ぐずりに入る。toy を持っていない時は長押しが無効
- [ ] 20秒以内に2回介入すると、2回目の満足度減少が2倍になる
- [ ] ステージ1を「介入だけ」で乗り切ろうとすると、90秒以内にヒヤリ3回に達する（介入が塞ぐことの代替にならない）
- [ ] 同じ toy で3回遊ぶと、回復量が 25→15→10 と減り、飽き時間が 30→40→50秒と伸びる
- [ ] 持っている toy が bored になって8秒経つと、赤ちゃんがその場に置いて別の目標へ向かう
- [ ] Loading 画面に豆知識が表示され、同一プレイ中に同じ豆知識が繰り返されない
- [ ] `verified: false` の豆知識が表示される場合、ビルド時に警告が出る（console.warn で可）
- [ ] §4.8 の表に挙げた状態変化すべてに、1秒以内の画面上の表現がある（表を上から順に手動で確認）
- [ ] `npm run sim:assert` が通る（§13.3 の全指標が目標範囲内）
- [ ] `random` ボット 1000 回で例外が出ない
- [ ] 同じシードで2回実行すると同じ結果になる（決定性の確認）
- [ ] 各Result画面で §8 の文言・出典・固定文が表示される
- [ ] `src/game/` から DOM / Canvas への参照が一つもない（grep で確認）

### フェーズ2
- [ ] `assets/` が空でも全ステージがプレースホルダーで遊べる
- [ ] glb を1つ置くとそのモデルだけ差し替わる
- [ ] 音声ファイルが無くても合成音が鳴り、ファイルを置くと差し替わる
- [ ] 3D上でも toy のドラッグが床平面上で機能する
- [ ] ミュートが機能し、初回クリック前に音が鳴らない
- [ ] `src/game/` がフェーズ1完了時点から変更されていない（git diff で確認）
- [ ] プレゼン用PCで 60fps 付近で動く

---

## 12. 決めていないこと（実装者判断でよい）

- 配色・フォント・カメラの正確な角度
- 絵文字のフォールバック文字
- ステージ3の双子の初期位置と cloth の配置
- パーティクルの粒数・色
- bgm_bored を入れるかどうか

---

## 13. 自動テストプレイ（ヘッドレス・シミュレーション）

`src/game/` がDOM・Canvasを参照しない設計は、この章のためにある。人間が遊ぶ前に、Node上でゲームを数千回回して難易度を目標範囲に入れる。

### 13.1 構成

- `sim/run.js`：`src/game/` の状態機械を Node で生成し、`dt = 1/60` で `timeLimit` 秒ぶん進める。描画層・音声層は null 実装を差し込む
- 乱数は **シード指定可能** にする（`src/game/` 内の `Math.random` 呼び出しは全て注入した rng 経由にする。これはフェーズ1ステップ1で対応）
- 1回の実行で `{ stageId, bot, seed, cleared, failAtSec, hiyari, playCount, interventions, comboHiyari, satisfactionCurve }` を返す
- `npm run sim -- --stage 1 --bot optimal --n 1000` で実行、結果は JSON と要約表を標準出力

### 13.2 ボット戦略（`sim/bots/`）

各ボットは毎フレーム `gameState` を読み、`input.js` が出すのと同じイベント（長押し開始/終了、ドラッグ開始/移動/終了）を発行する。

| bot | 挙動 | 何を検証するか |
|---|---|---|
| `noop` | 何もしない | 難易度の下限。放置で何秒持つか |
| `optimal` | 赤ちゃんの現在目標に最も近い未対策 hazard/item を順に塞ぐ。toy は赤ちゃんから遠い安全な床へ置き直す。介入は使わない | 上限。設計上クリア可能か |
| `intervene_only` | 塞がない。赤ちゃんが hazard に距離 60 まで近づいたら抱き上げて中央へ | 介入が塞ぐことの代替にならないこと |
| `random` | 0.5秒ごとにランダムな対象へランダムな操作 | 極端なバグ（例外・無限ループ）の検出 |
| `human_like` | `optimal` に反応遅延 0.4〜1.2秒、誤クリック率 10%、長押し中断率 15% を加える | 想定プレイヤーの体験。**主指標はこれ** |

### 13.3 指標と目標範囲

受け入れ条件（§11）のうち数値化できるものは、ここで自動判定する（`sim/assert.js`）。

| 指標 | 目標 |
|---|---|
| `noop` ステージ1 失敗までの秒数（中央値） | 30〜40秒 |
| `optimal` 全ステージ クリア率 | ≥ 95% |
| `intervene_only` ステージ1 失敗率 | ≥ 90% |
| `human_like` ステージ1 クリア率 | 70〜85% |
| `human_like` ステージ2 クリア率 | 50〜70% |
| `human_like` ステージ3 クリア率 | 35〜55% |
| `human_like` 各ステージ 遊び完了回数（中央値） | ≥ 2 |
| `human_like` ステージ2・3 組み合わせヒヤリの発生率（1回以上） | 30〜60%（起きなさすぎると学べず、起きすぎると理不尽） |
| `human_like` 満足度が 40 未満だった時間の割合 | 15〜40% |
| `random` 1000回 | 例外ゼロ、フレーム時間の異常なし |

### 13.4 チューニング手順

1. `TUNING` のうち **調整対象を5つに限定**：`BABY_SPEED`、`SAT_NO_TOY`、`FUSS_SEC`、`WEIGHT_TOY`、`fixSec`（全体倍率）。他は固定
2. 各変数を ±30% の範囲で3水準ずつ振り、`human_like` を各条件 300 回実行（合計 243 条件 × 300 回。数分で終わる想定）
3. 全指標が目標範囲に入る条件を列挙し、その中央付近を採用
4. 入る条件が無ければ、目標範囲か固定変数を見直して人間に報告する（勝手に目標を緩めない）
5. 採用値を `TUNING` に書き戻し、`npm run sim:assert` が通ることを確認してコミット

### 13.5 ブラウザ側の自動確認

- Playwright で `index.html` を起動し、Title → Loading → Tutorial → Play → StageResult の遷移をクリックで進める
- Play 中に長押し（`mousedown` → 待機 → `mouseup`）とドラッグを発行し、HUD の値が変化することを確認
- 各画面でスクリーンショットを保存。崩れの有無は人間または Claude が目視する
- 豆知識に `verified: false` が表示されていないことを DOM から確認

### 13.6 自動化できないこと（人間の確認項目）

- 面白いか、緊張感と理不尽感の区別
- 初見で何に迷うか
- 演出が不快でないか（特にぐずり・ヒヤリ）
- 当事者に見せて良いトーンか

人間の試行は、シミュレーションで指標が揃った後に **1〜2回** に絞る。感触の報告を受けて Claude Code が `TUNING` を再調整し、再度 `sim:assert` を通す、というループにする。
